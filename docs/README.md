# Documentation

## Structure

1. Documentation in the root of the `docs/` directory are living documents and should be preferred
2. Documentation in the `ai-artifacts/` directory represent AI-generated snapshots of state or intermediary works / inputs
