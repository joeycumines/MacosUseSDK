name: CI
on:
  push:
    branches:
      - main
  #pull_request:
  #  branches:
  #    - main
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    uses: ./.github/workflows/build.yaml
    secrets: inherit
  # Integration remains opt-in: it requires a permissioned self-hosted runner.
  #integration:
  #  if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
  #  needs: build
  #  uses: ./.github/workflows/integration.yaml
  #  secrets: inherit
